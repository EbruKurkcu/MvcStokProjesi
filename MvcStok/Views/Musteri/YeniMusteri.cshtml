@model MvcStok.Models.Entity.TBLMUSTERILER
@{
    ViewBag.Title = "YeniMusteri";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<form class="form-group" method="post">
    @*<div>
        <label>MÜŞTERİ ADI</label>
        <input type="text" class="form-control" name="MUSTERIAD" />
    </div>*@

    <div>
        @Html.LabelFor(m=>m.MUSTERIAD)
        @Html.TextBoxFor(m => m.MUSTERIAD, new {@class="form-control"})
        @Html.ValidationMessageFor(m => m.MUSTERIAD, "", new {@style="color:red"})
    </div>
    <br />
    <div>
        <label>MÜŞTERİ SOYADI</label>
        <input type="text" class="form-control" name="MUSTERISOYAD" />
    </div>

    <br />
    <button class="btn btn-warning" id="btn1">MÜŞTERİ EKLE</button>
    <script>
        $('#btn1').click(function () {
            alert('Müşteri Ekleme İşlemi Yapıldı...'); //Butona basıldığında alert mesajı penceresi açılacak.
        });
    </script>
</form>

